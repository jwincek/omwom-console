# Semaphore API connection
# Leave blank to run in mock mode (local development)
SEMAPHORE_URL=https://ops.omwom.com
SEMAPHORE_TOKEN=your-semaphore-api-token
SEMAPHORE_PROJECT_ID=1

# Modoboa API connection
# Leave blank to run in mock mode (local development)
# Generate a token in Modoboa: Admin panel → your user → API tokens
MODOBOA_URL=https://mail.omwom.com
MODOBOA_TOKEN=your-modoboa-api-token
